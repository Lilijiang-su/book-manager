<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookmanager.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM category ORDER BY sort_order, id
    </select>

    <select id="findByParentId" resultMap="BaseResultMap">
        SELECT * FROM category WHERE parent_id = #{parentId} AND status = 1 ORDER BY sort_order
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, description, parent_id, sort_order, status, create_time)
        VALUES (#{name}, #{description}, #{parentId}, #{sortOrder}, #{status}, NOW())
    </insert>

    <update id="update">
        UPDATE category
        SET name = #{name}, description = #{description}, parent_id = #{parentId}, sort_order = #{sortOrder}, status = #{status}
        WHERE id = #{id}
    </update>

    <update id="updateSortOrder">
        UPDATE category SET sort_order = #{sortOrder} WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE category SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <delete id="batchDelete">
        DELETE FROM category WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

</mapper>
