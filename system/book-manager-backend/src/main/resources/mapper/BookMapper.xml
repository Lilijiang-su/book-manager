<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookmanager.mapper.BookMapper">

    <resultMap id="BaseResultMap" type="Book">
        <id column="id" property="id"/>
        <result column="isbn" property="isbn"/>
        <result column="name" property="name"/>
        <result column="author" property="author"/>
        <result column="publisher" property="publisher"/>
        <result column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
        <result column="description" property="description"/>
        <result column="cover" property="cover"/>
        <result column="stock" property="stock"/>
        <result column="total" property="total"/>
        <result column="borrow_count" property="borrowCount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT b.*, c.name as category_name
        FROM book b
        LEFT JOIN category c ON b.category_id = c.id
        WHERE b.id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT b.*, c.name as category_name
        FROM book b
        LEFT JOIN category c ON b.category_id = c.id
        ORDER BY b.create_time DESC
    </select>

    <select id="search" resultMap="BaseResultMap">
        SELECT b.*, c.name as category_name
        FROM book b
        LEFT JOIN category c ON b.category_id = c.id
        WHERE (b.name LIKE CONCAT('%', #{keyword}, '%')
           OR b.author LIKE CONCAT('%', #{keyword}, '%')
           OR b.isbn LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY b.create_time DESC
    </select>

    <select id="findByCategoryId" resultMap="BaseResultMap">
        SELECT b.*, c.name as category_name
        FROM book b
        LEFT JOIN category c ON b.category_id = c.id
        WHERE b.category_id = #{categoryId}
    </select>

    <select id="countByCategoryId" resultType="long">
        SELECT COUNT(*) FROM book WHERE category_id = #{categoryId}
    </select>

    <select id="findWithFilters" resultMap="BaseResultMap">
        SELECT b.*, c.name as category_name
        FROM book b
        LEFT JOIN category c ON b.category_id = c.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (b.name LIKE CONCAT('%', #{keyword}, '%')
                   OR b.author LIKE CONCAT('%', #{keyword}, '%')
                   OR b.isbn LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="categoryId != null">
                AND b.category_id = #{categoryId}
            </if>
            <if test="stockStatus != null and stockStatus == 'available'">
                AND b.stock > 0
            </if>
            <if test="stockStatus != null and stockStatus == 'low'">
                AND b.stock > 0 AND b.stock &lt;= 2
            </if>
            <if test="stockStatus != null and stockStatus == 'empty'">
                AND b.stock = 0
            </if>
            <if test="startDate != null and startDate != ''">
                AND b.create_time >= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND b.create_time &lt;= #{endDate}
            </if>
        </where>
        ORDER BY b.create_time DESC
    </select>

    <!-- 分页查询 -->
    <select id="findPage" resultMap="BaseResultMap">
        SELECT b.*, c.name as category_name
        FROM book b
        LEFT JOIN category c ON b.category_id = c.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (b.name LIKE CONCAT('%', #{keyword}, '%')
                   OR b.author LIKE CONCAT('%', #{keyword}, '%')
                   OR b.isbn LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="categoryId != null">
                AND b.category_id = #{categoryId}
            </if>
            <if test="stockStatus != null and stockStatus == 'available'">
                AND b.stock > 0
            </if>
            <if test="stockStatus != null and stockStatus == 'low'">
                AND b.stock > 0 AND b.stock &lt;= 2
            </if>
            <if test="stockStatus != null and stockStatus == 'empty'">
                AND b.stock = 0
            </if>
            <if test="startDate != null and startDate != ''">
                AND b.create_time >= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND b.create_time &lt;= #{endDate}
            </if>
        </where>
        ORDER BY b.create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countWithFilters" resultType="long">
        SELECT COUNT(*)
        FROM book b
        <where>
            <if test="keyword != null and keyword != ''">
                AND (b.name LIKE CONCAT('%', #{keyword}, '%')
                   OR b.author LIKE CONCAT('%', #{keyword}, '%')
                   OR b.isbn LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="categoryId != null">
                AND b.category_id = #{categoryId}
            </if>
            <if test="stockStatus != null and stockStatus == 'available'">
                AND b.stock > 0
            </if>
            <if test="stockStatus != null and stockStatus == 'low'">
                AND b.stock > 0 AND b.stock &lt;= 2
            </if>
            <if test="stockStatus != null and stockStatus == 'empty'">
                AND b.stock = 0
            </if>
            <if test="startDate != null and startDate != ''">
                AND b.create_time >= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND b.create_time &lt;= #{endDate}
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book (isbn, name, author, publisher, category_id, description, cover, stock, total, status, create_time, update_time)
        VALUES (#{isbn}, #{name}, #{author}, #{publisher}, #{categoryId}, #{description}, #{cover}, #{stock}, #{total}, 1, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE book
        SET isbn = #{isbn}, name = #{name}, author = #{author}, publisher = #{publisher},
            category_id = #{categoryId}, description = #{description}, cover = #{cover}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateStock">
        UPDATE book SET stock = stock + #{delta} WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE book SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM book WHERE id = #{id}
    </delete>

    <delete id="batchDelete">
        DELETE FROM book WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <update id="batchUpdateCategory">
        UPDATE book SET category_id = #{categoryId}, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </update>

</mapper>
