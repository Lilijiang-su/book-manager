<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookmanager.mapper.AuditLogMapper">

    <resultMap id="BaseResultMap" type="AuditLog">
        <id column="id" property="id"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operator_name" property="operatorName"/>
        <result column="action" property="action"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="detail" property="detail"/>
        <result column="ip" property="ip"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_log (operator_id, operator_name, action, target_type, target_id, detail, ip, create_time)
        VALUES (#{operatorId}, #{operatorName}, #{action}, #{targetType}, #{targetId}, #{detail}, #{ip}, NOW())
    </insert>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM audit_log ORDER BY create_time DESC
    </select>

    <select id="findWithFilters" resultMap="BaseResultMap">
        SELECT * FROM audit_log
        <where>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="operatorName != null and operatorName != ''">
                AND operator_name LIKE CONCAT('%', #{operatorName}, '%')
            </if>
            <if test="startDate != null and startDate != ''">
                AND create_time >= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND create_time &lt;= #{endDate}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="findPage" resultMap="BaseResultMap">
        SELECT * FROM audit_log
        <where>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="operatorName != null and operatorName != ''">
                AND operator_name LIKE CONCAT('%', #{operatorName}, '%')
            </if>
            <if test="startDate != null and startDate != ''">
                AND create_time >= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND create_time &lt;= #{endDate}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countWithFilters" resultType="long">
        SELECT COUNT(*) FROM audit_log
        <where>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="operatorName != null and operatorName != ''">
                AND operator_name LIKE CONCAT('%', #{operatorName}, '%')
            </if>
            <if test="startDate != null and startDate != ''">
                AND create_time >= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND create_time &lt;= #{endDate}
            </if>
        </where>
    </select>

</mapper>
