# 个人图书借阅管理系统 — 黑白盒测试报告

- **测试日期**：2026-08-25
- **测试对象**：个人图书借阅管理系统（Spring Boot 3.4.4 + Vue 3 全栈）
- **测试方法**：黑盒（API 实际调用）+ 白盒（源码审计）
- **测试范围**：功能测试、安全测试、并发测试、边界测试

---

## 1. 测试环境

| 组件 | 版本/状态 |
|---|---|
| 后端 | Spring Boot 3.4.4，端口 8090 |
| 前端 | Vue 3 + Vite，端口 5173 |
| 数据库 | MySQL `book_manager` |
| Java / Maven | Java 26 / Maven 3.9.16 |

---

## 2. 测试摘要

| 类别 | 结论 |
|---|---|
| 核心功能（借还书/库存/CRUD） | ✅ 正常 |
| 并发控制（库存扣减/借阅上限） | ✅ 正确 |
| JWT 认证（签名/过期/算法混淆） | ✅ 安全 |
| SQL 注入防护 | ✅ 有效 |
| 前端 XSS 渲染防护 | ✅ 有效 |
| **授权控制（越权）** | ❌ **存在系统性缺陷** |
| **密码存储** | ❌ **明文存储** |
| **CORS 配置** | ❌ **任意来源+凭据** |

---

## 3. 高危漏洞（Critical/High）

### H1. 越权修改管理员账号
- **描述**：普通用户可调用 `/api/user/update` 修改任意用户（含管理员）的信息，无角色校验。
- **实测**：普通用户 `zhangsan` 成功篡改管理员 `admin` 的姓名/电话/邮箱。
- **影响**：任何登录用户可完全接管管理员账户，属决定性越权。
- **根因**：`UserController` 无角色权限校验，`UserService.update()` 无归属校验。

### H2. 接口级鉴权缺失
- **描述**：`WebConfig` 拦截器排除列表使用过宽通配符，导致写操作无鉴权。
- **实测**：
  - `POST /api/book/add` — 无 token 成功添加图书
  - `DELETE /api/book/{id}` — 无 token 成功删除图书
  - `PUT /api/fine-rule` — 无 token 成功修改罚款规则
- **根因**：排除列表含 `/api/book/**`、`/api/category/**`、`/api/fine-rule`，覆盖了写接口。

### H3. 密码明文存储 + 明文返回
- **描述**：数据库密码未哈希，登录接口把明文密码返回给前端。
- **实测**：登录响应含 `"password":"admin123"`。
- **根因**：`UserService.login()` 直接返回整个 User 实体；注册时明文存储；登录用明文 `equals` 比较。

### H4. CORS 任意来源 + 允许凭据
- **描述**：`allowedOriginPatterns("*")` + `allowCredentials(true)`。
- **实测**：带 `Origin: https://evil.com` 的请求返回 `Access-Control-Allow-Origin: https://evil.com` + `Allow-Credentials: true`。
- **影响**：恶意站点可构造携带用户 cookie 的跨域请求。

---

## 4. 中危漏洞（Medium）

| 编号 | 问题 | 实测 |
|---|---|---|
| M1 | 借阅记录无用户隔离 | 普通用户访问 `/api/borrow/list` 返回全量记录 |
| M2 | 错误信息泄露内部细节 | 返回 `Failed to convert value...`、SQL 异常堆栈 |
| M3 | 审计日志越权访问 | 普通用户可查看全部 669 条审计日志 |
| M4 | 罚款规则无鉴权可改 | 无 token 可 `PUT /api/fine-rule` |
| M5 | 删除分类无保护 | 删除含 4 子分类的父分类成功，子分类孤儿化 |
| M6 | 无输入长度校验 | 300 字符 ISBN 直接打到数据库，靠 DB 报错 |
| M7 | Excel 导出公式注入 | 导出未转义 `=`/`+`/`-`/`@` 前缀 |

---

## 5. 功能测试结果

| 功能 | 结果 |
|---|---|
| 登录 / 注册 | ✅ 正常 |
| 图书 CRUD | ✅ 正常（但无鉴权） |
| 借书（库存扣减） | ✅ 库存 8→7 正确 |
| 还书（库存恢复） | ✅ 状态→returned，库存恢复 |
| 借阅上限（每人 5 本） | ✅ 正确拦截 |
| 分类管理 | ✅ 基本正常 |
| 审计日志记录 | ✅ 正常（669 条） |
| 通知功能 | ✅ 正常（未读 39） |

## 6. 安全测试结果

| 测试项 | 结果 |
|---|---|
| JWT 伪造签名 | ✅ 被拒绝 |
| JWT `alg=none` 攻击 | ✅ 被拒绝 |
| JWT 过期 token | ✅ 被拒绝 |
| SQL 注入 `' OR 1=1--` | ✅ 未生效（参数化查询） |
| 存储型 XSS 注入 | ⚠️ 后端不过滤原样存储，前端插值转义兜底 |
| 前端 `v-html`/`innerHTML`/`eval` | ✅ 未使用 |
| 并发借书-库存超卖 | ✅ 无超卖（5 并发扣 5 库存） |
| 并发借阅上限绕过 | ✅ 被正确拦截 |

---

## 7. 修复建议（按优先级）

1. **【紧急】角色权限控制**：管理写接口加 `@RequireRole("admin")` 或拦截器校验角色。
2. **【紧急】修正拦截器排除列表**：只排除只读接口，写接口必须鉴权。
3. **【紧急】密码哈希**：改用 BCrypt，登录响应屏蔽 `password` 字段。
4. **【高】收紧 CORS**：限定前端实际域名。
5. **【中】借阅记录数据隔离**：按当前用户过滤。
6. **【中】错误信息脱敏**：生产环境不返回 SQL/类型细节。
7. **【中】删除分类保护**：有子分类/图书时拦截或级联确认。
8. **【中】输入长度校验**：后端加 `@Size` 等约束。
9. **【低】Excel 导出公式注入**：对 `=`/`+`/`-`/`@` 前缀转义。

---

## 8. 修复状态（2026-08-25 已修复并验证）

| 编号 | 修复内容 | 验证结果 |
|---|---|---|
| H1 | 拦截器 + Service 层双重角色校验，普通用户越权操作返回 403 | ✅ 全部 403 |
| H2 | 修正拦截器排除列表，写接口必须鉴权 | ✅ 无 token 写操作被拒 |
| H3 | BCrypt 密码哈希 + 登录响应屏蔽 password | ✅ 响应无密码字段 |
| H4 | CORS 限定 `localhost:5173` | ✅ 恶意 Origin 被拒 |
| M1 | 借阅记录按用户隔离 | ✅ 普通用户只见自己记录 |
| M2 | 全局异常脱敏 + 校验异常友好提示 | ✅ 500 不返回内部细节 |
| M3 | 审计日志仅 admin 可访问 | ✅ 普通用户 403 |
| M4 | 罚款规则 PUT 需鉴权 | ✅ 无 token 被拒 |
| M5 | 删除分类保护（子分类/图书检查） | ✅ 有子分类/图书被拒 |
| M6 | 实体/DTO 输入校验 + 全局处理 | ✅ 返回 400 + 具体消息 |
| M7 | Excel 导出公式注入转义 | ✅ 已加 `'` 前缀 |

**回归结果**：修复后 13 项安全/功能回归测试全部通过，核心功能（登录/借还书/库存/分类）正常。

---

## 9. 测试痕迹清理

- ✅ 越权添加的测试图书已删除
- ✅ XSS 测试图书/分类已删除
- ✅ 并发测试用户已删除
- ✅ admin 用户信息已恢复
- ✅ 分类树已恢复（原分类重建）

---

*报告由 Claude 在功能+安全测试后自动生成。*
